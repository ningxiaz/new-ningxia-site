//- A module for cover image
//- It takes one argument: the image path

mixin cover(path, spec)
  - spec = spec || {}
  - spec.__class = spec.__class || ''

  div(class=spec.__class)&attributes(attributes)
    .cover(style='background-image: url(' + path + ')')