mixin logo(baseUrl, spec)
  - spec = spec || {}
  - spec.__class = spec.__class || ''

  div(class=spec.__class)&attributes(attributes)
    a(href='/')
      img(src= baseUrl + 'images/logo.svg', class='logo-img')
